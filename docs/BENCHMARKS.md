# Local Embedding Benchmarks

TODO: Re-run benchmarks with the current backends (FastEmbed on CPU, MLX on
Apple Silicon, llama.cpp ROCm on AMD GPU, llama.cpp CUDA on NVIDIA GPU,
Lemonade FLM on AMD Ryzen AI NPU) and document the methodology and results
here.

The previous content covered an older Apple Silicon backend (Core ML /
llama.cpp Metal) that has since been replaced by MLX, so the numbers were
no longer representative.
