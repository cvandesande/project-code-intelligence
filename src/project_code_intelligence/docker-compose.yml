name: project-code-intelligence

services:
  pgvector:
    profiles: ["db"]
    image: pgvector/pgvector:pg18
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${PGVECTOR_DB:-codeintel}
      POSTGRES_USER: ${PGVECTOR_USER:-codeintel}
      POSTGRES_PASSWORD: ${PGVECTOR_PASS:-codeintel}
    ports:
      - "${PROJECT_CODE_INTELLIGENCE_BIND_HOST:-127.0.0.1}:${PGVECTOR_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGVECTOR_USER:-codeintel} -d ${PGVECTOR_DB:-codeintel}"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - pgvector-data:/var/lib/postgresql
      - ./docker/pgvector/init-extensions.sql:/docker-entrypoint-initdb.d/init-extensions.sql:ro

  fastembed:
    profiles: ["cpu"]
    build:
      context: .
      dockerfile: docker/fastembed/Dockerfile
    environment:
      PROJECT_CODE_INTELLIGENCE_FASTEMBED_MODEL: ${PROJECT_CODE_INTELLIGENCE_FASTEMBED_MODEL:-jinaai/jina-embeddings-v2-base-code}
      PROJECT_CODE_INTELLIGENCE_FASTEMBED_CACHE_DIR: /models/fastembed
      PROJECT_CODE_INTELLIGENCE_FASTEMBED_HOST: 0.0.0.0
      PROJECT_CODE_INTELLIGENCE_FASTEMBED_PORT: 18081
    ports:
      - "${PROJECT_CODE_INTELLIGENCE_BIND_HOST:-127.0.0.1}:${PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT:-18081}:18081"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:18081/healthz', timeout=2).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 30
    volumes:
      - fastembed-models:/models/fastembed

  lemonade-npu:
    profiles: ["npu"]
    image: ${PROJECT_CODE_INTELLIGENCE_LEMONADE_IMAGE:-ghcr.io/lemonade-sdk/lemonade-server:latest}
    command: ["./lemond", "--host", "0.0.0.0", "--port", "13305"]
    environment:
      HF_TOKEN: ${HF_TOKEN:-}
      PROJECT_CODE_INTELLIGENCE_EMBEDDING_ENDPOINT_MODEL: ${PROJECT_CODE_INTELLIGENCE_LEMONADE_EMBEDDING_MODEL:-embed-gemma-300m-FLM}
    ports:
      - "${PROJECT_CODE_INTELLIGENCE_BIND_HOST:-127.0.0.1}:${PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT:-18081}:13305"
    devices:
      - /dev/accel/accel0:/dev/accel/accel0
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - lemonade-huggingface:/root/.cache/huggingface
      - lemonade-llama:/opt/lemonade/llama
      - lemonade-cache:/root/.cache/lemonade

  llama-rocm:
    profiles: ["amdgpu"]
    build:
      context: .
      dockerfile: docker/llamacpp-rocm/Dockerfile
      args:
        ROCM_BASE_IMAGE: ${PROJECT_CODE_INTELLIGENCE_ROCM_BASE_IMAGE:-debian:stable-slim}
    environment:
      PROJECT_CODE_INTELLIGENCE_AMD_GFX: ${PROJECT_CODE_INTELLIGENCE_AMD_GFX:-}
      PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_BUNDLE: ${PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_BUNDLE:-}
      PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_RELEASE: ${PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_RELEASE:-latest}
      PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_REPO: ${PROJECT_CODE_INTELLIGENCE_LLAMA_CPP_ROCM_REPO:-lemonade-sdk/llamacpp-rocm}
      PROJECT_CODE_INTELLIGENCE_HF_MODEL_REPO: ${PROJECT_CODE_INTELLIGENCE_HF_MODEL_REPO:-Qwen/Qwen3-Embedding-0.6B-GGUF}
      PROJECT_CODE_INTELLIGENCE_HF_MODEL_FILE: ${PROJECT_CODE_INTELLIGENCE_HF_MODEL_FILE:-Qwen3-Embedding-0.6B-Q8_0.gguf}
      PROJECT_CODE_INTELLIGENCE_LLAMA_MODEL: ${PROJECT_CODE_INTELLIGENCE_LLAMA_MODEL:-/models/Qwen3-Embedding-0.6B-Q8_0.gguf}
      PROJECT_CODE_INTELLIGENCE_EMBEDDING_HOST: 0.0.0.0
      PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT: 18081
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_CTX: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_CTX:-40960}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_BATCH: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_BATCH:-2048}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_UBATCH: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_UBATCH:-1024}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_PARALLEL: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_PARALLEL:-4}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_N_GPU_LAYERS: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_N_GPU_LAYERS:-999}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_EXTRA_ARGS: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_EXTRA_ARGS:-}
      HF_TOKEN: ${HF_TOKEN:-}
    ports:
      - "${PROJECT_CODE_INTELLIGENCE_BIND_HOST:-127.0.0.1}:${PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT:-18081}:18081"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:18081/v1/models"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 10m
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    ipc: host
    security_opt:
      - seccomp=unconfined
    volumes:
      - llamacpp-rocm:/opt/llamacpp-rocm
      - ./models:/models

  llama-cuda:
    profiles: ["nvidia"]
    build:
      context: .
      dockerfile: docker/llamacpp-cuda/Dockerfile
      args:
        CUDA_BASE_IMAGE: ${PROJECT_CODE_INTELLIGENCE_CUDA_BASE_IMAGE:-ghcr.io/ggml-org/llama.cpp:server-cuda}
    environment:
      PROJECT_CODE_INTELLIGENCE_HF_MODEL_REPO: ${PROJECT_CODE_INTELLIGENCE_HF_MODEL_REPO:-Qwen/Qwen3-Embedding-0.6B-GGUF}
      PROJECT_CODE_INTELLIGENCE_HF_MODEL_FILE: ${PROJECT_CODE_INTELLIGENCE_HF_MODEL_FILE:-Qwen3-Embedding-0.6B-Q8_0.gguf}
      PROJECT_CODE_INTELLIGENCE_LLAMA_MODEL: ${PROJECT_CODE_INTELLIGENCE_LLAMA_MODEL:-/models/Qwen3-Embedding-0.6B-Q8_0.gguf}
      PROJECT_CODE_INTELLIGENCE_EMBEDDING_HOST: 0.0.0.0
      PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT: 18081
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_CTX: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_CTX:-40960}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_BATCH: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_BATCH:-2048}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_UBATCH: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_UBATCH:-1024}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_PARALLEL: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_PARALLEL:-4}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_N_GPU_LAYERS: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_N_GPU_LAYERS:-999}
      PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_EXTRA_ARGS: ${PROJECT_CODE_INTELLIGENCE_LLAMA_SERVER_EXTRA_ARGS:-}
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
      HF_TOKEN: ${HF_TOKEN:-}
    ports:
      - "${PROJECT_CODE_INTELLIGENCE_BIND_HOST:-127.0.0.1}:${PROJECT_CODE_INTELLIGENCE_EMBEDDING_PORT:-18081}:18081"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:18081/v1/models"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 10m
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    ipc: host
    volumes:
      - ./models:/models

volumes:
  pgvector-data:
  fastembed-models:
  lemonade-huggingface:
  lemonade-llama:
  lemonade-cache:
  llamacpp-rocm:
