"""Compatibility facade for embedding helpers."""

from __future__ import annotations

from project_code_intelligence.embedding.core import (
    embed_items_with_retry,
    embed_texts_once,
    embedding_input_text,
    embedding_retry_min_chars,
    smaller_embedding_max_chars,
)
from project_code_intelligence.embedding.endpoint import (
    embed_with_endpoint,
    embedding_endpoint_hint,
    embedding_headers,
    embedding_index,
    embedding_request_retries,
    embedding_request_timeout_seconds,
    endpoint_host_is_loopback,
    http_error_detail,
    is_context_size_error,
    parse_embedding_items,
    preflight_embedding_endpoint,
    read_embedding_response,
    resolve_embedding_endpoint_model,
    retry_sleep_seconds,
    validate_embedding_endpoint,
    vector_literals_from_items,
)
from project_code_intelligence.embedding.preembedding import (
    abandon_preembedding,
    code_preembedding_enabled,
    consume_preembedding_results,
    embed_record_batch,
    insert_records_with_preembedding,
    mark_record_embedding_skipped,
    next_preembedding_result,
    preembedding_ahead_batches,
    preembedding_worker,
    start_record_preembedding,
)
from project_code_intelligence.embedding.store import (
    EMBEDDING_CONTRACT_METADATA_KEY,
    database_retries,
    embed_db_records,
    embedding_contract,
    embedding_contract_from_metadata,
    embedding_metadata,
    embedding_snapshot_counts,
    existing_embedding_contract_from_rows,
    fetch_embedding_batch,
    object_int_value,
    require_compatible_embedding_contract,
    row_embedding_contract,
    row_int_value,
    run_database_operation,
    run_database_operation_attempt,
    set_snapshot_embedding_contract,
    snapshot_embedding_contract,
    update_embedding_batch,
    validate_embedding_compatibility,
    vector_literal_dimensions,
)
from project_code_intelligence.embedding.types import (
    EmbeddingBackend,
    EmbeddingEndpointUnavailable,
    EmbeddingEndpointUnavailableError,
    EmbeddingRow,
    EmbeddingRunConfig,
    SkippedEmbeddingRow,
)

__all__ = [
    "EMBEDDING_CONTRACT_METADATA_KEY",
    "EmbeddingBackend",
    "EmbeddingEndpointUnavailable",
    "EmbeddingEndpointUnavailableError",
    "EmbeddingRow",
    "EmbeddingRunConfig",
    "SkippedEmbeddingRow",
    "abandon_preembedding",
    "code_preembedding_enabled",
    "consume_preembedding_results",
    "database_retries",
    "embed_db_records",
    "embed_items_with_retry",
    "embed_record_batch",
    "embed_texts_once",
    "embed_with_endpoint",
    "embedding_contract",
    "embedding_contract_from_metadata",
    "embedding_endpoint_hint",
    "embedding_headers",
    "embedding_index",
    "embedding_input_text",
    "embedding_metadata",
    "embedding_request_retries",
    "embedding_request_timeout_seconds",
    "embedding_retry_min_chars",
    "embedding_snapshot_counts",
    "endpoint_host_is_loopback",
    "existing_embedding_contract_from_rows",
    "fetch_embedding_batch",
    "http_error_detail",
    "insert_records_with_preembedding",
    "is_context_size_error",
    "mark_record_embedding_skipped",
    "next_preembedding_result",
    "object_int_value",
    "parse_embedding_items",
    "preembedding_ahead_batches",
    "preembedding_worker",
    "preflight_embedding_endpoint",
    "read_embedding_response",
    "require_compatible_embedding_contract",
    "resolve_embedding_endpoint_model",
    "retry_sleep_seconds",
    "row_embedding_contract",
    "row_int_value",
    "run_database_operation",
    "run_database_operation_attempt",
    "set_snapshot_embedding_contract",
    "smaller_embedding_max_chars",
    "snapshot_embedding_contract",
    "start_record_preembedding",
    "update_embedding_batch",
    "validate_embedding_compatibility",
    "validate_embedding_endpoint",
    "vector_literal_dimensions",
    "vector_literals_from_items",
]
